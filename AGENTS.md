<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# USELESS PEOPLE Agent Instructions

This is intentionally a useless project. Do not make it useful.

Permanent rules for every coding agent:

- Preserve the absurd narrative premise of USELESS PEOPLE.
- The human is the creative director and owns design decisions, character selection, story content, jokes, timing, colors, typography, and final UI choices.
- Do not independently redesign, sanitize, commercialize, professionalize, or reinterpret the project.
- Do not introduce unsolicited features.
- Do not use generic AI-generated visual patterns, SaaS layouts, dashboards, pricing sections, feature grids, glassmorphism, arbitrary gradients, or stock visual filler unless the human asks for them.
- Favor small, reviewable implementation steps.
- Do not modify unrelated working functionality.
- Never expose or commit credentials, tokens, passwords, API keys, Supabase secrets, deployment credentials, or other private values.
- Always test meaningful changes before considering a task complete.

Current Phase 1 scope:

- Keep character data local.
- Use Next.js App Router, TypeScript, Tailwind CSS, and Framer Motion.
- Do not add a database, auth, Supabase, OpenAI API, GSAP, login, signup, or user accounts.
- Build route skeletons and maintainable foundations only.
- Keep visuals neutral and easy for the human to redesign unless preserving confirmed existing direction.
